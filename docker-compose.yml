version: '3'
services:
  event_registration_api:
    build:
      context: ./
    volumes:
      - .:/src
    ports:
      - 3000:3000
    restart: unless-stopped
    environment:
      - MONGO_HOST=survey_app_mongodb:27017